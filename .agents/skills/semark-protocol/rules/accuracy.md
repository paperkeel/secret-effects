# Accuracy

Update a signature in the same change that changes its documented behavior. Apply this
rule to responsibility, behavior, inputs, results, side effects, failures, invariants,
boundaries, relationships, and public roles.

Treat the source code as the implementation authority. Treat a missing or inaccurate
signature as a code-quality defect.
